object Transpose {

    fun transpose(input: List<String>): List<String> {
        // 1. find which item is the longest
        val max = input.map { it.length }.max()

        return emptyList()
    }
}
